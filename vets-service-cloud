spring:
  datasource:
    url: ${vcap.services.vets-db.credentials.jdbcUrl:jdbc:mysql://localhost:3306/petclinic?useSSL=false}
    username: ${vcap.services.vets-db.credentials.username:root}
    password: ${vcap.services.vets-db.credentials.password:petclinic}
