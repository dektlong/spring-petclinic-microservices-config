spring:
  datasource:
    url: ${vcap.services.visits-db.credentials.jdbcUrl}&statementInterceptors=brave.mysql.TracingStatementInterceptor&zipkinServiceName=visits-db
    username: ${vcap.services.visits-db.credentials.username}
    password: ${vcap.services.visits-db.credentials.password}
